# Server
PORT=3000

# Ollama endpoint (local host from native OS)
# If you run locally without Docker, you can also use: http://localhost:11434
OLLAMA_URL=http://host.docker.internal:11434

# API key required by this gateway in Authorization: Bearer <key>
GATEWAY_API_KEY=changeme-local-key
